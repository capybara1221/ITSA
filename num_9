#include <stdio.h>
#include <string.h>
#include <math.h>
#include <ctype.h>

// 9
// 基礎題目3： 判斷座標是否在圓形的範圍內
int main(void)
{
    int x,y,r=200;
    while(scanf("%d %d",&x,&y) != EOF)
    {
        if((x*x+y*y)<=40000)
            printf("inside\n");
        else
            printf("outside\n");
    }
}
